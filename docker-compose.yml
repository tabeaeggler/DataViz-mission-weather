version: '3.7'
services:
  react:
    stdin_open: true 
    tty: true
    container_name: react_prod
    build:
      context: client
      dockerfile: Dockerfile
    ports:
      - 3001:3001
    environment:
      - CHOKIDAR_USEPOLLING=true
